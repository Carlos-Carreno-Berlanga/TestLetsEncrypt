{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft": "Warning",
      "Microsoft.Hosting.Lifetime": "Information"
    }
  },
  "AllowedHosts": "*",
  "LetsEncrypt": {
    // Set this to automatically accept Let's Encrypt's terms of service.
    // If you don't set this in config, you will need to press "y" whenever the application starts
    "AcceptTermsOfService": true,

    // You must at least one domain name
    "DomainNames": [ "carcarbe.chickenkiller.com", "192.168.1.132" ],

    // You must specify an email address to register with letsencrypt.org
    "EmailAddress": "carcarbe@protonmail.ch",
    "UseStagingServer": "false"
  }
}
